<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
    <style>
        body {
            margin: auto 0;
        }

        #Left {
            float: left;
            width: 100px;
            margin-left: 10px;
        }

        #Right {
            float: left;
        }

        #centre {
            position: relative;
            width: 100%;
            height: 500px;
        }

        #tail {
            position: absolute;
            bottom: 5px;
        }

        body, ul, li, ol, p, dl, dd, h1, h2, h3, h4, h5, h6, img, iframe, form, div {
            margin: 0;
            padding: 0;
            list-style-type: none;
            border: 0;
        }

        #BookSummary {
            width: 750px;
            height: 530px;
        }


        #BookSummary, li, a {
            text-decoration: none;
        }

        .LI_img {
            width: 150px;
            display: block;
            height: 150px;
        }

        #BookSummary, li {
            float: left;
            width: 150px;
            margin-right: 47px;
            margin-bottom: 16px;
            height: 250px;
        }

        /*#BookSummary, li  .author {
            padding: 0 20px;
            height: 24px;
            line-height: 24px;
            color: #aaa;
        }*/
    </style>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="navbar navbar-inverse" role="navigation">
            <a class="navbar-brand" href="#">衣服商店 </a>
            @*@Html.Action("Summary", "Cart")
                @Html.Action("Summary", "Customer")
                @using (Html.BeginForm("List", "Product", FormMethod.Get))
                {

                    <div>

                        <input type="text" name="SearchString" placeholder="Search" />
                        <button class="btn" type="submit"><i class="icon-search"></i></button>
                    </div>

                }*@

        </div>
    </div>
    <div id="centre">
        <div id="Left" class="row panel">
            <div id="categories" class="col-xs-3">
                @Html.Action("Menu", "Nav")
            </div>
        </div>
        <div id="Right" class="col-xs-8">
            @RenderBody()
        </div>
    </div>
    <div id="tail">16136133姚鹏</div>
    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>